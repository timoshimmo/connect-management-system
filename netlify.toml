[build]
  base = "frontend"
  # Fix: Modified to run frontend's build script directly inside this directory
  command = "npm install --prefix ../backend && npm run build"
  publish = "dist"
  ignore = "exit 1"

[functions]
  directory = "../backend/functions"
  node_bundler = "esbuild"
  external_node_modules = [
    "serverless-http",
    "@aws-sdk/client-s3",
    "@aws-sdk/s3-request-presigner",
    "bcrypt",
    "compression",
    "cookie-parser",
    "cors",
    "dotenv",
    "express",
    "express-rate-limit",
    "helmet",
    "jsonwebtoken",
    "mongoose",
    "multer",
    "nodemailer",
    "pino",
    "pino-http",
    "swagger-jsdoc",
    "swagger-ui-express",
    "zod"
  ]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

